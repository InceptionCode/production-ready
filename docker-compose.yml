services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: production
      DATABASE_URL: /app/data/tasks.db
      LOG_LEVEL: INFO
      PORT: "8000"
    volumes:
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
